const addTwoNumbers = require("./CommonJS Module.js")

console.log(`Sum of 2 and 5 is ${addTwoNumbers(2, 5)}`)

// In modern way, usign ECMAScript Module System, we do it like:
// import { addTwoNumbers } from "./CommonJS Module.js"